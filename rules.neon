parameters:
    ignoreErrors:
        # 忽略仓储测试中的 getRepository 调用 - 仓储测试需要通过 EntityManager 获取仓储
        -
            message: '#Instead of getting repository from EntityManager, use constructor injection and service pattern to keep code clean#'
            paths:
                - tests/Integration/Repository/*RepositoryTest.php